# Mainnet Status Board

Latest aggregate verification: 2026-08-04T19:08:46Z - `npm.cmd run proof:prelaunch:summary` passed every required local row. It reports 24 external/status blockers and G1-G14 at 0/14 Complete; the blockers remain visible for deployed V10 metadata identity, stale authorization-ready Preview, env/signoff/host/indexer/restore/monitoring/QA evidence, live canary/soak, backup, strict chain RPC, and launch proof. The V10 dry-run Preview remains read-only (`transactionSent=false`), and no transaction, wallet signing, deployment, secret, or private-RPC action occurred.

Current state: local launch-proof tooling is consistent, but external production/canary evidence is still missing. Mainnet launch is not approved until G1-G14 are Complete in both this board and docs/mainnet-proof-record.md.

Last local verification: 2026-08-04 - `npm.cmd run check:summary` passed lint, business logic, security follow-up, fetch-timeout and stored-number parsing tests, V9/V10 contract tests, indexer storage, DB operations, runtime monitoring tests, production build, typecheck, HTTP smoke, and browser smoke. The local smoke server used `http://127.0.0.1:3101` and no listener remained on port 3101 afterward. The launch-local proof baseline still requires the L1-L17 `npm.cmd run proof:local:summary` gate, including `proof:files`, `proof:remaining`, `proof:gates -- --structure-only`, `proof:launch-map`, `proof:launch-docs`, `proof:readiness`, mainnet proof output guard, residual security follow-up 8/8 with `appResolveEpochFiles=0`, production dependency audit high/critical pass, all-dependency audit with the documented known dev-toolchain exception, and strict launch expected-fail coverage; `proof:launch` and `proof:launch -- --strict` remain blocked until G1-G14 evidence and the live canary log exist; G1-G14 remain Missing pending external evidence. This is local build/test/smoke evidence only; it does not approve wallet signing, transactions, live canary/soak, deploys, ABI changes, randomness changes, tokenomics changes, private RPC/env reads, or secret handling.

Latest remaining launch evidence verification: 2026-08-04T15:11:34Z - `npm.cmd run proof:remaining:summary` reports `Complete gates: 0/14`, remaining groups `canary=2`, `chain=1`, `env=1`, `host=2`, `indexer=1`, `monitoring=1`, `qa=3`, `restore=1`, `signoff=2`, no inconsistent rows, and zero complete-evidence, required-proof, proof-record-reference, or first-check issues. The next gate remains `G1 Final contract env and funds safety`; autonomous work remains `local-hardening-only`. The same command now prints the transaction boundary `fresh-preview-plus-explicit-consent`, the required pre-transaction Preview checks including `npm.cmd run preview:canary:v10:authorization-ready:summary`, and the explicit consent requirement before any real bets, claims, resolver actions, approvals, nonce replacements, or soak starts.

Latest aggregate verification: 2026-08-04T17:50:44Z - `npm.cmd run proof:autonomous:summary` passed in read-only mode after refreshing the V10 dry-run Preview. `docs/v10-canary-dry-run-preview.md` was updated at 2026-08-04T17:47:34.174Z with dry-run evidence from `data/live-test-runs/live-canary-2026-08-04T17-47-32-208Z.jsonl`: `transactionSent=false`, `signingMaterialLoaded=false`, `walletClientCreated=false`, `contractWriteSubmitted=false`, `dryRunProofBlocksG10G11=true`, `transactionLimit=7`, `estimatedGas=566008`, and `plannedBetTx=12`. The separate `preview:canary:v10:authorization-ready:summary` row passed while fresh (`maxAgeMinutes=15`), but this only proves freshness for a future separate bounded consent and does not authorize bets, claims, approvals, nonce replacement, resolver actions, or soak. `npm.cmd run proof:prelaunch:summary` completed with all required local rows passed and 24 external/status blockers still visible; G1-G14 remain 0/14 Complete launch gates. The aggregate rows still surface residual security follow-up 8/8 with `hostAuth=true`, `webLocks=true`, `keeperNonce=true`, `keeperBotReceipts=true`, `depositLimiter=true`, `dryRunDefaults=true`, `ciSecurity=true`, `autoResolve=true`, and `appResolveEpochFiles=0`; browser auto-resolve remains fail-closed before `/api/bootstrap-resolve` unless a read-only funded-pool precheck succeeds; ABI/indexer storage keeps `sameBlockEventOrdering=true`, `staleEventReplayIgnored=true`, `staleEpochReplayIgnored=true`, and `staleFinancialReplayIgnored=true`; wallet runtime logic, claim state machines, API boundaries, auth boundaries, replica rate-limit boundaries, typecheck, lint, production build, bundle baseline, SQLite operations, monitoring drill, process model, dependencies, cleanup dry-run, launch docs, proof templates/drafts/files, launch map, host guard, gate structure, and readiness structure passed as local evidence only.
That aggregate baseline passed all required local rows; its external/status blocker count is historical evidence only and does not complete any G1-G14 gate.

Latest dependency local verification: 2026-08-03 - production dependency audit briefly exposed a new blocking high advisory through the transitive `@metamask/sdk -> socket.io-client -> socket.io-parser@4.2.6` path. The repo now pins `socket.io-parser` to `4.2.7`, which is the current fixed version and remains inside `socket.io-client@4.8.3`'s `~4.2.4` range. Verified with `npm.cmd ls socket.io-parser socket.io-client @metamask/sdk`, `npm.cmd run proof:deps:summary` (`total=26`, `high=0`, `blocking=0`), `npm.cmd run proof:deps:all:summary` (`total=38`, `knownDev=9`, `blocking=0`), `npm.cmd run proof:wallet-deps:summary`, `npm.cmd run proof:autonomous:summary`, and `npm.cmd run proof:prelaunch:summary`.

Latest blocker visibility verification: 2026-08-03 - `proof:autonomous:summary` and `proof:prelaunch:summary` now show the chain strict blocker as `issue=strict-chain-proof-requires-configured-rpc-env` and the backup blocker as `issue=backup-paths-or-source-output-required`. This keeps G1/G8 external blockers compact without promoting them to proof or reading private RPC/env values.

Latest daily hygiene verification: 2026-08-03 - `npm.cmd run proof:autonomous:daily:summary` passed in read-only mode with no transactions, no deploys, and no cleanup apply. Production dependency audit has `total=26`, `high=0`, `critical=0`, and `blocking=0`; all-dependency audit has `total=38`, 9 known dev-toolchain high exceptions, and 0 blocking high/critical issues; Privy/Wagmi/Viem dependency integrity is present, CI security reports read-only permissions, SHA-pinned actions, no `pull_request_target`, and no persisted checkout credentials. Bundle baseline remains within budget with `files=225`, `totalBytes=8422245`, `jsBytes=7028521`, `largestJsBytes=1040081`, `largestJsFile=static/chunks/2098-d46557e0e0ad5f5e.js` under the `maxSingleJsBytes=1250000` guard, `cssBytes=216200`, and `wasmBytes=1056860`; cleanup dry-run matched 0 delete targets. This does not change the 0/14 launch-gate status.

Latest V10/indexer/DB local verification: 2026-08-04 - `npm.cmd run proof:security-followup:summary` passed 8/8 with `appResolveEpochFiles=0`; `npm.cmd run test:contract:v10:summary` passed with `assertionFailures=0`, 22 state-changing entrypoints, `protocolFeeFlushCases=7`, `protocolFeeFlushEntrypointCases=4`, duplicate/rebate/dust/rollback/timelock/packed-boundary/full-range accounting coverage, and V10 runtime bytes 16488; `npm.cmd run bench:contract:v10:compiler-matrix:summary` passed 16/16 profiles with canonical runtime headroom 8088; `npm.cmd run proof:contract-deployed:v10:offline:summary` passed manifest/executable-runtime identity with `transactionSent=false`; `npm.cmd run test:indexer-storage:summary` passed scope isolation, idempotent upsert, normalized event isolation, `staleEventReplayIgnored=true`, `staleEpochReplayIgnored=true`, `staleFinancialReplayIgnored=true`, `depositScopeIsolation=true`, `idempotentDepositUpsert=true`, `resolverRewardScopeIsolation=true`, `idempotentResolverRewardUpsert=true`, `dustSettlementScopeIsolation=true`, `idempotentDustSettlementUpsert=true`, `epochScopeIsolation=true`, `idempotentEpochUpsert=true`, `jackpotScopeIsolation=true`, `idempotentJackpotUpsert=true`, `rewardClaimScopeIsolation=true`, `idempotentRewardClaimUpsert=true`, `singleRebateClaimParity=true`, partial/malformed log fallback, limited reads, and same-block event ordering; `npm.cmd run test:db-operations:summary` passed local backup, retention, strict rejection, restore, disk-full, and corrupt-startup cases. This is local evidence only and does not close G1-G14.

Latest UX/accessibility local verification: 2026-08-03 - dialog focus trapping now excludes `hidden`, `display:none`, `visibility:hidden`, `visibility:collapse`, disabled, `aria-disabled`, and disabled-fieldset controls before applying the existing `aria-hidden` and `inert` subtree checks. White Paper decorative hero particles also wait for `motionReady`, do not render when `reducedMotion` is enabled, and remain `aria-hidden`. Verified with `node --check scripts/test-business-logic.mjs`, `npm.cmd run test:logic:summary`, `npm.cmd run typecheck:summary`, `npm.cmd run lint:summary`, `npm.cmd run build:summary`, and `npm.cmd run baseline:bundle:summary`. This changed shared modal focus behavior plus decorative docs animation behavior only and does not close wallet QA or mobile HTTPS evidence gates.

Latest proof redaction local verification: 2026-08-03 - shared proof-output redaction now covers whitespace-separated sensitive CLI arguments such as `--private-key value`, `--rpc-url value`, `--database-url value`, and `--webhook-url value` without consuming the next option token when the value is missing. `proof:autonomous:summary` includes a dedicated `proof collector redaction` row before the broad wallet-runtime proof row, and `proof:prelaunch:summary` now parses the same child result before the generic `Summary:` fallback. Both routine rows summarize it as strict counters: `status=pass`, `cases=6`, `redacted=5`, `leaked=0`, `issues=0`. Verified with syntax checks, `npm.cmd run proof:collector-redaction:summary`, `npm.cmd run test:logic:summary`, `npm.cmd run proof:autonomous:summary`, and `npm.cmd run proof:prelaunch:summary`. This hardens local proof/log output only and does not close external evidence gates.

Latest canary proof-validator local verification: 2026-08-04 - `scripts/analyze-live-canary-proof.mjs` now fails strict canary proof on duplicate successful role/epoch keys scoped by role, chain ID, contract address, and epoch, while preserving the explicit `repeat=true` same-signature fee-measurement exception. It also fails strict proof on duplicate successful nonce keys scoped by role, chain ID, contract address, `nonceLatest`, and `noncePending`, and rejects failed `mode=preflight` readiness events so balance/allowance/nonce blockers remain visible. Verified with syntax checks, `npm.cmd run proof:drafts:summary` (`total=258`, `rejected=245`), `npm.cmd run test:logic:summary`, and the expected blocked `npm.cmd run proof:testnet:canary:v10:summary` missing live-log result. This strengthens G10/G11 evidence validation only; real canary logs are still missing and G10/G11 remain Missing.

## Autonomous Work Boundary

Autonomous local work may improve proof tooling, redaction, docs, contract invariants, ABI/indexer compatibility, runtime checks, UX/accessibility guards, performance diagnostics, and cleanup safety. It must not mark any G1-G14 row Complete.

External evidence is still required for production/canary host proof, Privy origin proof, real wallet QA, real canary logs, backup/restore artifacts, monitoring/email alert artifacts, direct chain/indexer reconciliation, and operator sign-off.

| ID | Gate | Required proof | First check | Status |
| --- | --- | --- | --- | --- |
| G1 | Final contract env and funds safety | docs/signoff-proof.json with concrete contractEnv, chain ID, token, deploy block, finality, V10 protected bets flag, operator sign-off evidence, and existing saved artifacts | npm.cmd run proof:mainnet -- --strict --out=docs/mainnet-env-proof.log | Missing |
| G2 | Owner Safe or multisig | docs/signoff-proof.json ownership section with concrete ownership.directOwnerReadEvidence, Safe/multisig governance evidence or proof tx, and existing saved artifacts | npm.cmd run proof:signoff -- --strict | Missing |
| G3 | Randomness model sign-off | docs/signoff-proof.json randomness section with randomness.decision, operator/signer sign-off evidence, and existing saved artifacts | npm.cmd run proof:signoff -- --strict | Missing |
| G4 | Chain reconciliation | docs/signoff-proof.json chainComparison with concrete direct-chain and app/indexer evidence for jackpot, safetyPool, deposits, rewards, rebates, resolve, and existing saved artifacts | npm.cmd run proof:chain -- --strict --out=docs/chain-proof-snapshot.json | Missing |
| G5 | Production process model | docs/host-proof.json showing lore-site, lore-bot, lore-indexer supervised separately with concrete supervisor evidence and persistent DB evidence | npm.cmd run proof:host:collect -- --origin=https://playlore.xyz --host-type=production --load-origin=https://canary.playlore.xyz --load-host-type=canary --db-path=<absolute-external-LORE_DB_PATH> --supervisor=<pm2-systemd-docker-compose> --process-evidence=docs/host-process-model.log --health-log=docs/host-health-prod.log --load-log=docs/host-load-http.log --out=docs/host-proof.draft.json | Missing |
| G6 | Production health and load | docs/host-proof.json with concrete docs/host-health-prod.log health:prod evidence including base=<production origin> and numeric finalityLagBlocks, concrete docs/host-load-http.log load:http evidence with Load base URL: matching the staging/canary origin, and externalRateLimit evidence proving webReplicaCount/distinctReplicas/sharedBucketVerified/failClosed across two replicas | npm.cmd run proof:host -- --strict | Missing |
| G7 | Indexer fresh DB dry-run | docs/indexer-proof.json from fresh external DB and deploy block with concrete dry-run, INDEXER_FINALITY_BLOCKS, docs/indexer-once.log, finalityLagBlocks health evidence, chainSnapshot rpcChainId/contractAddress evidence, and chainComparison evidence | npm.cmd run proof:indexer:collect -- --fresh-db=true --epochs=<count> --chain-id=59144 --deploy-block=<deploy-block> --finality-blocks=<finality-blocks> --indexer-log=docs/indexer-once.log --health-log=docs/indexer-health-prod.log --chain-snapshot=docs/chain-proof-snapshot.json --out=docs/indexer-proof.draft.json | Missing |
| G8 | Backup and restore drill | docs/restore-proof.json with concrete backupSchedule, retentionDays, lastSuccessfulBackupAt, docs/restore-backup-schedule.log, docs/restore-drill.log, docs/restore-health-prod.log, docs/restore-indexer-preservation.log, finality-aware restored health, indexerPreservation evidence, and existing saved artifacts | npm.cmd run proof:restore:collect -- --source=<absolute-source-db-outside-repo> --backup-dir=<absolute-backup-dir-outside-repo> --restore-dir=<absolute-restore-dir-outside-repo> --backup=<absolute-backup-file-inside-backup-dir> --restored-origin=https://restore.playlore.xyz --restored-host-type=restore --restore-log=docs/restore-drill.log --health-log=docs/restore-health-prod.log --backup-schedule-artifact=docs/restore-backup-schedule.log --preservation-artifact=docs/restore-indexer-preservation.log --out=docs/restore-proof.draft.json | Missing |
| G9 | Monitoring and error tracking | docs/monitoring-proof.json with complete enabled health-prod, data-sync, stale-indexer-heartbeat, indexer-lag, bot-restart, indexer-restart, reverted-tx monitors, plus docs/monitoring-alert-export.log, docs/monitoring-recovery-export.log, docs/monitoring-alert-target-test.log, docs/error-tracking-test-event.log, fired/recovery alerts, verified email alert target, and error event evidence | npm.cmd run proof:monitoring:plan -- --provider=<provider> --error-provider=<error-provider> --origin=https://playlore.xyz --out=docs/monitoring-alert-test-plan.draft.md | Missing |
| G10 | Real canary epochs | docs/canary-proof.json and live canary log with target-RPC JSONL, successful role coverage for MANUAL, AUTOMINER_A, and AUTOMINER_B, 50 successful auto-miner unique epochs, unique tx hashes, and concrete target/session evidence | npm.cmd run proof:canary -- data/live-test-runs/live-canary-YYYY.jsonl --strict | Missing |
| G11 | Transaction recovery safety | docs/canary-proof.json and live canary log proving noDuplicateBets, noNonceLoops, noStuckPending, pendingRecoveryConverged, and concrete recovery evidence | npm.cmd run proof:canary -- data/live-test-runs/live-canary-YYYY.jsonl --strict | Missing |
| G12 | Wallet QA | docs/qa-proof.json with concrete artifact-like evidence for Privy allowed origins, redacted production App ID configured proof, connect/disconnect/reconnect, wrong network, mobile Web3 browser, clean-wallet first tx, and slow auth | npm.cmd run proof:qa:plan -- --origin=https://playlore.xyz --network=linea-mainnet --chain-id=59144 --out=docs/qa-canary-test-plan.draft.md | Missing |
| G13 | Failure UX and audit visibility | docs/qa-proof.json with concrete artifact-like evidence for disabled reasons, pending states, degraded data, bet history, auto-miner logs, and diagnostics | npm.cmd run proof:qa -- --strict | Missing |
| G14 | Final launch QA | docs/qa-proof.json plus live canary log and concrete debug autominer smoke, mobile layout, overlays, chat geometry, mainnet wording, fresh final security scan, and no open High/Medium local findings evidence | npm.cmd run proof:files -- --canary-log=<canary-log-file> | Missing |
